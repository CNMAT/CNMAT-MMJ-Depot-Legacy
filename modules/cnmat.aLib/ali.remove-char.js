/*by Ali MomeniCopyright UC Regents*/anything.immediate = 1;var badchar = jsarguments[1];//post("badchar: ", badchar);//post();function anything() {	var string = arrayfromargs(messagename,arguments);	string = string[0];	// The point of the above, instead of string=messagename, is so you can send in numbers as well as symbols.//	post("this: ", string);//	post();	var tstring = "";	string = '' + string;	splitstring = string.split(badchar);	for(i = 0; i < splitstring.length; i++)	tstring += splitstring[i];	outlet(0, tstring);}